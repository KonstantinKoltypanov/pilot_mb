version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: mbpilot-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
